═══════════════════════════════════════════════════════════════
    AIClaude Frontend 一键部署指南
    服务器 IP: 43.251.225.146
    域名: aiclaude.online
═══════════════════════════════════════════════════════════════

【第一步：配置 DNS】★★★ 必须先完成！★★★

登录您的域名注册商（阿里云/腾讯云/GoDaddy等）:

1. 添加 A 记录:
   主机记录: @
   记录类型: A
   记录值: 43.251.225.146
   TTL: 600

2. 添加 CNAME 记录:
   主机记录: www
   记录类型: CNAME
   记录值: aiclaude.online
   TTL: 600

等待 5-10 分钟让 DNS 生效

═══════════════════════════════════════════════════════════════

【第二步：上传文件】

使用 MobaXterm:

1. SSH 连接到: root@43.251.225.146
2. 将整个 aiclaude-react 文件夹拖到 /root/
3. 等待上传完成

═══════════════════════════════════════════════════════════════

【第三步：执行部署】

在 MobaXterm 终端执行:

cd /root/aiclaude-react
chmod +x deploy.sh setup-ssl.sh
./deploy.sh

等待 5-10 分钟，看到 "🎉 部署完成！" 即成功

═══════════════════════════════════════════════════════════════

【第四步：验证访问】

打开浏览器访问: http://aiclaude.online

用户现在可以直接输入 aiclaude.online 访问您的网站了！

═══════════════════════════════════════════════════════════════

【可选：启用 HTTPS】

在服务器上执行:

cd /root/aiclaude-react
docker-compose stop nginx
sudo ./setup-ssl.sh

选择 1) 自动申请 Let's Encrypt 证书

证书申请成功后，编辑配置:

nano nginx.conf

找到第 46 行，取消注释:
# return 301 https://$server_name$request_uri;
改为:
return 301 https://$server_name$request_uri;

找到第 65-120 行的 HTTPS server 块，删除所有 # 号

保存退出 (Ctrl+X, Y, Enter)

重启服务:
docker-compose up -d

现在访问: https://aiclaude.online

═══════════════════════════════════════════════════════════════

【常用命令】

查看状态:       docker-compose ps
查看日志:       docker-compose logs -f
重启服务:       docker-compose restart
停止服务:       docker-compose stop
更新部署:       docker-compose down && docker-compose build --no-cache && docker-compose up -d

═══════════════════════════════════════════════════════════════

【故障排查】

1. 无法访问?
   - 检查防火墙: sudo ufw allow 80 && sudo ufw allow 443
   - 查看日志: docker-compose logs -f
   - 检查容器: docker-compose ps

2. DNS 未生效?
   - 测试解析: ping aiclaude.online
   - 等待更长时间（最多 24 小时）
   - 清除本地 DNS 缓存

3. SSL 失败?
   - 确保 80 端口未占用: docker-compose stop nginx
   - 重新申请: sudo ./setup-ssl.sh

═══════════════════════════════════════════════════════════════

【部署架构】

Internet
   ↓
aiclaude.online (DNS → 43.251.225.146)
   ↓
Nginx 容器 (80/443 端口)
   ↓
Next.js 容器 (3000 端口)

═══════════════════════════════════════════════════════════════

【重要文件说明】

deploy.sh               - 一键部署脚本
setup-ssl.sh            - SSL 证书申请脚本
docker-compose.yml      - Docker 编排配置
Dockerfile              - 镜像构建文件
nginx.conf              - Nginx 配置
服务器部署配置.md        - 详细配置文档
DEPLOYMENT.md           - 完整部署指南

═══════════════════════════════════════════════════════════════

🎉 完成后，用户输入 aiclaude.online 即可访问您的前端主页！

详细文档请查看: 服务器部署配置.md
